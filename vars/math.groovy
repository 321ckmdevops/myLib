def sum(a,b)
{
  echo "The result of ${a} and ${b} is ${a+b}"
}
def diff(a,b)
{
  echo "The result of ${a} and ${b} is ${a-b}"
}
def mul(a,b)
{
  echo "The result of ${a} and ${b} is ${a*b}"
}
def div(a,b)
{
  echo "The result of ${a} and ${b} is ${a/b}"
}
